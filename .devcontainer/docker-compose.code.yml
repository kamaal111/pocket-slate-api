services:
  code:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: pocket-slate-api-code
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
